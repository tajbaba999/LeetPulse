info:
  name: Signin
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/v1/signin
  body:
    type: json
    data: |-
      {
        "email" : "tajbaba105@gmail.com",
        "password" : "Taj@123@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

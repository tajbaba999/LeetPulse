meta {
  name: Sync Stream (SSE)
  type: http
  seq: 4
  doc: Real-time sync progress via Server-Sent Events. Paste accessToken in the token query param.
}

get {
  url: {{baseUrl}}/api/v1/codingprofile/sync/stream?token={{accessToken}}
  body: none
  auth: none
}

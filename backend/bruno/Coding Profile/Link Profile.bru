meta {
  name: Link Profile
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/codingprofile
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "leetcode": "tajbaba999"
  }
}

meta {
  name: Ingest
  type: http
  seq: 1
  doc: Re-ingest RAG from Postgres data (no LeetCode re-fetch).
}

post {
  url: {{baseUrl}}/api/v1/rag/ingest
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

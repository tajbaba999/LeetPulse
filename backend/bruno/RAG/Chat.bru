meta {
  name: Chat
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/rag/chat
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "question": "What topics am I weakest in?"
  }
}

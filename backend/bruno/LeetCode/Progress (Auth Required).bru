meta {
  name: Progress (Auth Required)
  type: http
  seq: 8
  doc: Paginated solved problems list. Requires LEETCODE_SESSION + LEETCODE_CSRF in .env.
}

get {
  url: {{baseUrl}}/api/v1/leetcode/progress
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

# Automatically merged by Docker Compose in local dev.
# For production: docker compose -f docker-compose.yml up -d
services:
  api:
    build:
      target: dev
    volumes:
      - ./src:/app/src
    environment:
      NODE_ENV: development

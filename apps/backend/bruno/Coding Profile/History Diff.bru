meta {
  name: History Diff
  type: http
  seq: 6
  doc: Compares two snapshots to show progress between them. Uses latest two by default.
}

get {
  url: {{baseUrl}}/api/v1/codingprofile/history/diff
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
